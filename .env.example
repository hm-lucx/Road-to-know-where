# Optional, aber fuer echte Tankstellenpreise empfohlen.
# Auf Render/Railway als Environment Variable setzen, nicht in Git committen.
TANKERKOENIG_API_KEY=dein_tankerkoenig_api_key

# Lokal optional. Hosting-Plattformen setzen PORT normalerweise automatisch.
PORT=5000
